<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Edit Employee</h1>
    <p class="text-sm text-gray-500 mt-1"><%= @employee.full_name %> (<%= @employee.employee_code %>)</p>
  </div>

  <%= render "form", employee: @employee, url: admin_employee_path(@employee), method: :patch %>
</div>
