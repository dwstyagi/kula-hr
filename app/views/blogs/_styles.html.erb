<style>
  .blog-prose {
    color: #57534e;
    font-size: 1.0625rem;
    line-height: 1.85;
  }

  .blog-prose > * + * { margin-top: 1.5rem; }

  .blog-prose h2 {
    margin-top: 3.5rem;
    color: #0c0a09;
    font-size: 1.75rem;
    font-weight: 700;
    letter-spacing: -.02em;
    line-height: 1.25;
    scroll-margin-top: 6rem;
  }

  .blog-prose h3 {
    margin-top: 2.5rem;
    color: #1c1917;
    font-size: 1.25rem;
    font-weight: 700;
    letter-spacing: -.01em;
    scroll-margin-top: 6rem;
  }

  .blog-prose strong { color: #1c1917; font-weight: 650; }

  .blog-prose a {
    color: #2f6a55;
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: #a7cfbf;
  }

  .blog-prose a:hover { color: #17392d; text-decoration-color: #2f6a55; }

  .blog-prose ul, .blog-prose ol { padding-left: 1.35rem; }
  .blog-prose ul { list-style: disc; }
  .blog-prose ol { list-style: decimal; }
  .blog-prose li { margin-top: .6rem; padding-left: .35rem; }
  .blog-prose li::marker { color: #79716b; }

  .blog-prose blockquote {
    border-left: 3px solid #36775f;
    padding: .25rem 0 .25rem 1.5rem;
    color: #44403c;
    font-style: italic;
  }

  .blog-prose code {
    background: #f5f5f4;
    border: 1px solid #e7e5e4;
    border-radius: .3rem;
    padding: .1rem .35rem;
    font-size: .9em;
    color: #17392d;
  }

  .blog-prose table {
    width: 100%;
    border-collapse: collapse;
    font-size: .95rem;
    font-variant-numeric: tabular-nums;
  }

  .blog-prose thead th {
    background: #fafaf9;
    border-bottom: 2px solid #d6d3d1;
    padding: .75rem .9rem;
    text-align: left;
    color: #17392d;
    font-weight: 650;
  }

  .blog-prose tbody td {
    border-bottom: 1px solid #e7e5e4;
    padding: .75rem .9rem;
    vertical-align: top;
  }

  .blog-prose img,
  .blog-prose svg {
    display: block;
    width: 100%;
    height: auto;
    margin: 2.5rem 0;
    border-radius: .75rem;
  }

  .blog-prose img {
    border: 1px solid #e7e5e4;
    background: #fafaf9;
  }

  .blog-prose figure { margin: 2.5rem 0; }
  .blog-prose figure img, .blog-prose figure svg { margin: 0; }

  .blog-prose figcaption {
    margin-top: .85rem;
    color: #79716b;
    font-size: .8125rem;
    line-height: 1.6;
    text-align: center;
  }

  .blog-prose hr {
    border: 0;
    border-top: 1px solid #e7e5e4;
    margin-top: 3rem;
  }

  .blog-table-wrap { overflow-x: auto; }

  .blog-tab {
    border-radius: 9999px;
    padding: .4rem .95rem;
    color: #c8e3d8;
    font-size: .8125rem;
    font-weight: 600;
    line-height: 1.4;
    transition: background-color .15s, color .15s;
  }

  .blog-tab:hover { background: rgba(255, 255, 255, .1); color: #fff; }

  .blog-tab-active { background: #fff; color: #17392d; }
  .blog-tab-active:hover { background: #fff; color: #17392d; }

  .blog-rail {
    position: sticky;
    top: 6.5rem;
    align-self: start;
    max-height: calc(100vh - 8rem);
    overflow-y: auto;
  }

  .blog-toc-link {
    display: block;
    color: #79716b;
    font-size: .8125rem;
    line-height: 1.5;
    transition: color .15s;
  }

  .blog-toc-link:hover { color: #2f6a55; }

  .blog-prose > *:first-child { margin-top: 0; }
  .blog-prose h2:first-child { margin-top: 0; }

  .blog-prose details summary::-webkit-details-marker { display: none; }
</style>
